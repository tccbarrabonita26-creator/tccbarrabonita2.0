namespace BarraBonitaTurismo.Data
{
    /// <summary>
    /// Seed removido intencionalmente.
    /// Os dados são gerenciados exclusivamente pelo painel administrativo.
    /// </summary>
    public static class DbInitializer
    {
        public static void Seed(AppDbContext db)
        {
            // Nenhum dado inicial é inserido automaticamente.
            // Use o painel admin em /Admin/Login para cadastrar atrações, eventos, etc.
        }
    }
}
